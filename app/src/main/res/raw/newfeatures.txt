* Added a service to download the radar images
* Integrated the radar into an overlay on a GoogleMap that zooms to the current location
* The radar is using a new Google Cloud Run service, let me know if there are problems
* Updates to dependencies
* Update to target SDK version as per Google's requirements